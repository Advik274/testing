[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "cd client && npm install && npm run build && cd ../server && npm install && npm run seed && npm start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[variables]
  PORT = "8080"
  NODE_ENV = "production"
